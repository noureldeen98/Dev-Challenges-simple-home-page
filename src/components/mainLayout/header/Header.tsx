import React from "react";

const Header = () => {
  return (
    <div className="flex flex-row ">
      {/* Logo */}
      {/* Menu in large screens */}
      {/* Mobile Menu */}
    </div>
  );
};

export default Header;
